# 03 · 当前规划与进行中

> 最后更新：2026-09-13（**新增 7.1.2 成本可观测 + prompt 瘦身（已完成）；新增 7.1.3 凡人修仙传成本基线实测（推翻 3 条原优化假设）；WAL 实测生效**）
> 方向已定：**不另起炉灶，在当前项目上做"切片式重写"**（理由见 §0）。

---

## 0. 为什么是切片式重写，不是全量重写

2026-09-09 已论证并拍板。核心理由（防止日后动摇）：

1. **架构没崩，崩的是工程纪律。** `router/service/core` 分层清晰、gateway 适配器模式正确、上下文引擎设计合理。缺的是测试/日志/变更记录——换仓库这些还是没有。
2. **全量重写有死亡谷**：写到 60–70% 时旧的不能忍、新的不能写稿，项目多半死在这段。
3. **硬踩过的坑会重踩**：04 里记的模型行为坑（思考字段厂商差异、重复惩罚逼崩采样…）是几十次调试换来的，重写等于清零。
4. **AI 结对特性**：AI 擅长"给定契约的单模块重写"，极不擅长跨全仓重构。切片是顺应为之。

**切片式重写的含义**：每个模块**内部允许 100% 推倒重写**，但必须一次一个、接口契约不动、写完有测试。

---

## 1. 已拍板的模块处置（2026-09-09）

| 模块 | 决定 | 理由 |
|---|---|---|
| **伏笔 `foreshadows`** | ✅ **保留并补齐写入** | 长篇最核心价值（人记不住跨几十章的伏笔）。AI 已在抽，只差落库 |
| **阶段摘要 `stage_summaries`** | ✅ 保留，**暂不动** | 代码真实；写到 30 章以上会变刚需 |
| **全局配置 `app_configs`** | ✅ 保留，**零动作** | 真实工作中，0 行是因为走默认值 |
| **角色技能 `skills`** | ✅ 保留 | CRUD 完整且与角色级联绑定，删除成本 > 收益 |
| **走向 `directions`** | ✅ **已删除（2026-09-10）** | 删了 ORM 类 / 3 条桩路由（`routers/direction.py`）/ 前端 `api/direction.js` / `schemas/direction.py` / DB 表（0 行）/ 顺带清掉因此孤立的 `stubs.recommend_directions`。**能力不丢**：`ingestion.py` 每章仍推走向卡片到商讨面板（`ChatPanel.vue` 渲染 `meta.type=post_chapter_directions`，未动）。路由 142 → 139 |
| ~~**大纲 / 套路模板 `outlines`**~~ | ⏸ **撤销删除，改为冻结** | **2026-09-09 修正**：流水线（网文量产）恰恰最需要套路模板。之前的判断"模板没价值"是错的。真正的问题是：当前 7 条模板**硬编码 + 题材绑定 + 不可编辑**，与 01「题材无关」原则冲突。**重做方向**：可编辑/可新增/题材中立的结构化模板（节奏骨架 / 情节节点 / 冲突类型 / 爽点分布）。**实现方式待考究，目前冻结不动** |

---

## 2. 整改路线图

### ✅ 已完成 Phase（状态已确认至 02-已实现清单 §1）

> 详细实施记录已移至 `docs/_archive/Phase0-4与R-实施记录归档-2026-09-12.md`（需要历史细节再查）。

| Phase | 内容 | 完成 |
|---|---|---|
| Phase 0 立规矩 | pytest 骨架 / logging 全量替换 / CHANGELOG / 五文档定稿 | 2026-09-10 |
| Phase 1 止血 | 隐私日志清理 / 错误不落库正文 / resp.ok 校验 / /validate 显式 501 / 占位页转诚实页 | 2026-09-10 |
| Phase 2 主闭环补齐 | 伏笔回注（闭环贯通）/ 章节列表接线 / 走向接下一章 / 级联删除补孤儿 / 对话可中断 / 历史孤儿清理 / 摄取分档 | 2026-09-10 |
| Phase 3 工程质量 | 死代码清理 / 商讨两阶段去重 / SSE 与 SVG 公共化 / SSE 帧编码与模型解析统一 / 107 处 broad-except 补日志 | 2026-09-10 |
| Phase 4 Agent 闭环 | eval 版本留档+打分+对比 / token 计量 + trace_id 贯通 / 章节改动反馈回流 | 2026-09-10 |
| Phase R 检索升级 | bge-m3 / sqlite-vec / Hybrid RRF / rerank / GraphRAG 一跳 / 按需加载 e2e | 2026-09-10 |

### 📌 已完成 Phase 的遗留待办（尚未做完，别丢）

| 来源 | 待办 |
|---|---|
| Phase 4 | ① **用量只覆盖章节生成** —— 商讨 / 写后摄取 / 指令解析的模型调用尚未计量（统计偏低）；② 走向的「采纳 / 拒绝」反馈（需前端交互）；③ 多维度打分（`eval_records.dimensions` 字段已预留） |
| Phase R | 可选增强：GraphRAG **两跳**（当前刻意只一跳，防窗口爆）；A5 实体图扩展到商讨路径 |

### Phase 5 · 体验（可增量）

分页 / 表单校验 / v-loading / 404 / 删除回收站 / 导出 txt / 全文搜索

### Phase 6 · 工具调用（function calling）（2026-09-10 规划，**待拍板**）

**核心判断：项目不缺「工具调用」，缺的是「闭环」。**
现状有 4 种手搓替代品，**全部单向**——模型说完、后端执行，**模型拿不到结果**，无法据此续答或多轮：

| 现有交互 | 用在哪 | 硬伤 |
|---|---|---|
| **文本标记协议** `LOAD_REFS:` / `LOAD_SETTING:` | 商讨按需加载 | 正则抠 id，脏输出即漏；小模型把握不住"该不该请求" |
| **JSON 抽取** | `/角色` `/地点` 指令、写后摄取记忆抽取 | 一次性；模型**不知道落库结果**（重名被跳过？字段缺了？） |
| **人工编排 DAG** | 工作流 8 种节点 | **模型不能自主决定调用哪个节点**，顺序写死在图里 |
| **SKILL 文本注入** | `skill_dispatch` | 只是 prompt 拼接，谈不上"调用" |

**已撞到的天花板（证据）**：`routers/chapter.py:254` 注释 —— 章节生成**主动放弃** AI 两阶段 LOAD_REFS，
理由「本地 4b 模型在该协议上不稳定，**时而全不加载、时而一股脑全加载**」，改用确定性打分 top-up。

**已有基础设施（关键，改造是插拔式的）**：`services/reference_selector.py` 已定义 **A/B 策略抽象**——
`MarkerSelection`（A，现状默认）/ `ToolCallSelection`（B，**占位未实现**，连"未来实现要点"都写在注释里）。
且明确「**无论 A 还是 B，之后的 fetch/inject/Pass2 完全共用**」→ 换策略不动主链路。

#### 扩展方向（按性价比排序）

| 优先级 | 方向 | 现状痛点 | 落地要点 |
|---|---|---|---|
| **P0** | `retrieve_refs` / `load_setting` 工具化 | 靠正则抠文本标记 | 实现 `ToolCallSelection` + 适配器加 `chat_with_tools()` |
| **P0/P1** | **实体 / 伏笔入库工具**（`upsert_character`、`plant_foreshadow`、`create_location`） | 模型收不到落库反馈 → 无法自我纠正 | 工具返回「已创建 / 重名跳过，是否改名」，模型据此调整 |
| **P1** | **检索类工具**（`search_chapters`、`get_character_state`、`list_open_foreshadows`、`query_timeline`） | 上下文引擎"一股脑塞 + budget 裁剪" | **省 token 的关键**：改成模型主动取，比"预塞+裁剪"更准更省 |
| **P2** | 一致性检查（`check_consistency` / `lookup_setting`，纯只读） | 长程设定自相矛盾 | 只读工具，安全，可先上 |
| **P2** | 工作流节点暴露为工具 | DAG 只能人工编排 | 从 DAG 走向 Agent；**风险高，靠后** |
| **P3** | 写操作类（改章节 / 扩大纲） | — | **必须 dry-run + 用户确认**，不许模型直改稿 |

> **附带收益**：P0/P1 的实体/伏笔工具会**顺带修复已记录的"伏笔断链"**（`foreshadow_actions` 曾被抽取但从不写入 `ForeshadowORM`；Phase 2.1 已用非工具路径补上，工具化后模型能感知结果）。

#### 工程配套（不做这些会翻车）

1. **适配器抽象**：`BaseModelAdapter` 加 `chat_with_tools()`。三家字段不同——OpenAI `tools`/`tool_calls`、Claude `tools`/`tool_use`、Ollama 仅部分模型支持
2. **必须保留 marker 降级**（抽象层已具备，正好用上）
3. **Agent 循环**：步数上限（建议 5）、超时、可中断（复用现有 `stop` 机制）
4. **SSE 事件**：新增 `tool_call` / `tool_result` → 前端显示"正在查资料库…"，否则是黑盒
5. **观测**：工具名 / 参数 / 耗时 / 结果大小落库（复用 `load_observation` 思路）
6. **日志**：工具调用必须记日志（呼应 Phase 3.5 铁律）
7. **成本**：只给"按需取数"类工具，**别把全量 schema 常驻 prompt**

#### ★ 前提：取决于主力模型（先确认再动工）

- **主力云端**（ModelScope Qwen3.5 / GLM）：原生 tool_calls 成熟，**值得做**
- **主力本地 4b**：连 marker 协议都不稳，指望它填对 `tool_calls` 的 JSON 参数更不可行 →
  **方向应转为确定性算法**（同 `chapter.py` 当时的取舍），别硬上

#### 🔴 边界约束（2026-09-12 增补 —— 定这条是为了防滥用）

**FC 只用于「步数不确定的探索型取数」。确定性取数一律后端预取，不给模型自主权。**

判据（三问，任一为"否"就走预取）：
1. 取哪些数据，是否**依赖模型自己的推理过程**才确定？
2. 取数次数/顺序是否**无法事先写死**？
3. 模型拿到第一批结果后，是否**可能改变**下一批要取什么？

反例（**不该用 FC**）：7.3.5 的"回归理由材料包"—— 谁是蛰伏角色（casting 已算）、他哪章消失（台账已算）、
其后梗概（一个 SQL），**三步全确定** → 用 FC 等于把确定任务交给不确定组件，
与 `chapter.py:254` 当年放弃 AI 两阶段 `LOAD_REFS` 是同一个坑。
正例：`retrieve_templates(query)` —— 模型才知道自己该查什么（见 7.6）。

> 附带约束：**章节生成链路继续用确定性 top-up，不做 agent 化**（03 原话："那条链要的是稳定不是自主性"）。
> 给生成层加自主性 = 把已经修好的问题主动修回去。

#### 建议推进顺序（3 步，每步可独立验收）

1. **`retrieve_refs` 工具化** —— 成本最低，且能**先验证原生 tool_calls 在你的模型上到底稳不稳**
2. 稳了再做**实体 / 伏笔入库** —— Agent 味最强，附带修断链
3. 最后做**检索类工具**省 token

> **状态：待拍板**。与 Phase 4.3（反馈回流）主题相通 —— 都是让系统从"执行器"变成"会自我改进的 Agent"。
> 关联：01 §7（工具调用条目）、05 §11（现有交互模式与工具层设计）。

### Phase 7 · 模板库与规划驱动写作（2026-09-11 立项，**已拍板**）

**立项背景（用户对现状的判断，原话要点）**：当前生成链路本质上只服务「已写完全书大纲、甚至每章梗概」的作者
—— **输入侧（规划）是空的**。卡文/水情节的根因 = 缺「章计划」这个中间层：一篇的情节概括只有几百字，
直接扩成几万字必然发散失控；不扩则两三章过完一篇，太平淡。真正缺的是**别人验证过的「几百字 → 9 章」拆法**，
且同一节拍要有多种走法可供借鉴（卡文时看别人怎么走）。

**已拍板（2026-09-11）**：
1. 入库管线「人工合并」步骤 = **AI 给候选组 + 用户点确认**（全自动凝练会产出平庸均值与错误归类）；
2. 起步 **20 本**书，但**先用 Qwen3-8B（硅基流动 API，零成本档）跑 1~2 本验证概括/切分质量**再放量
   （管线模型走 `model_configs`，可随时切换）；
   > ⚠️ **2026-09-12 更正**：此处原写"Ollama 本地"，与代码不符 —— 实测 `plot_import.py` 直连
   > **硅基流动 `Qwen/Qwen3-8B`**（`SF_BASE=https://api.siliconflow.cn/v1`，`enable_thinking=false`）；
   > **弧归并走 DeepSeek `deepseek-flash`**（升档模型，非 8B）。
3. 章计划**用新表** `article_plans`（不复活冻结的 outlines）；
4. 成本事实（2026-09-11 核实）：FC 闭环最坏 5 步吃满前缀缓存 ≈ **0.025 元**，与现状一次生成持平 ——
   **经济不是约束，延迟与模板数据质量才是**。

#### 7.0 路线图总览（2026-09-12 收敛 —— 几轮讨论后的正式顺序，以此为准）

讨论沉淀出的**四层资产模型**与**四大支柱机制**见 `docs/01 §3.5`（唯一权威定义）。
各阶段顺序与状态：

| 顺序 | 阶段 | 内容 | 状态 |
|---|---|---|---|
| **前置** | **7.1.1 入库管线补全** | `--stage all` 纳入 **anonymize** + 凝练加 **cast** + `--stage verify` 自检 + WAL/busy_timeout + 投递单更新 | ✅ **完成（2026-09-12 真机端到端）**；WAL 已于 **2026-09-13 实测生效** |
| **前置** | **7.1.2 成本可观测 + prompt 瘦身**（2026-09-13 新增） | 离线管线用量记账（含缓存命中拆分）+ `_plan_prompt` 模板瘦身 | ✅ **完成（2026-09-13 真机验证）** —— 详见文末 7.1.2 |
| **前置** | **7.1.3 成本基线实测**（2026-09-13 新增） | 凡人修仙传 1~30 章真机跑批 + 成本分布分析 | ✅ **完成（232s，44 条记账）** —— **推翻 3 条原优化假设**，详见文末 7.1.3 |
| 1 | **7.1 模板库基建** | 概括（批量+并发+续跑）→ **匿名化归一** → 段切分（断点续跑）→ 弧归并 → 凝练 → 向量检索 | ✅ 完成（详见下方实施记录） |
| 2 | **7.2 篇规划流（后端）** | `article_plans` + 模板检索生成计划 + 行级修改 + 拍板 + `layer_chapter_plan` 生成联动 | ✅ 后端完成（端到端真机验证）；**前端待做 → 顺延为 7.5** |
| 3 | **7.3 角色向量选角** | 槽位人格化（cast desc 入模板）→ 槽位向量库 → 计划向量选角 → casting 一次锁定 → 阈值标定 → 出场校验 **+ 角色状态门（候选池过滤）** | ✅ **已实现（2026-09-13，实施记录见文末 7.3 小节）** |
| 4 | **7.3.5 角色连续性**（2026-09-12 新增） | 新角色引入链路 + 消失判定（explicit/silent）+ 回归理由（确定性预取）+ 篇间交接校验 | ✅ **B 档已实现（2026-09-13，实施记录见文末 7.3.5 小节）**；C 档（status 自动抽取 + UI）待做 |
| 5 | **7.4 数据放量与跨书验证** | OpenCode 按 100 章/段喂数据（斗破余 1471 章）→ 跨书聚类验证 → 检索/选角效果重测 | 🟡 进行中（斗破 200/1671）；**降为后台并行**（跑批不占开发主线，见 7.0.1） |
| 6 | **7.5 前端规划页** | 结构化表格 + 行级"AI 改这行" + 拍板按钮 + casting 可视化 **+ 引入单确认 + 交接警告** | 🟡 **v1 已实现（2026-09-13，拆解 1~5 步一次到位）**——侧栏「篇规划」入口 + 路由 `plan`；vite build 通过、真机交互待用户验证；C 档角色状态展示（步骤 6）待做 |
| 7 | **7.6 批量生成器 + FC 闭环** | 原 7.3/7.4 顺延 | ⚪ 待做（在 7.5 之后） |

> 编号说明：下方历史小节标题沿用记录时的编号（7.1/7.2/7.2.1 等），**以本表顺序为准**。
> 原计划「7.3 批量生成器 / 7.4 FC 闭环」顺延为 7.6。
> **2026-09-12 增补**：新增 `7.1.1`（并行灌数据前置）与 `7.3.5`（角色连续性）；
> 7.3 的验收范围扩大为「能选对角 + 不选死人」（A 档状态门并入 7.3，理由见 7.3.5 开头）。

#### 7.0.1 接下来规划（2026-09-13 7.3.5 收官后拍板）

**现状一句话**：模板库 → 计划 → 选角 → 连续性四层后端能力已齐（345 单测全绿，核心链路真机验证），
但**全部没有前端入口** —— 正撞上本项目已踩两次的「零入口病」（`config-chat`、`chapters`：
代码完整、路由存在、侧栏无菜单项，用户看不到 = 不存在）。且 7.3.5 的核心交互
（确认建卡 / unmatched 槽位关联 / 交接差集三选一）**本质是作者拍板**，没有 UI 等于没做。

**三线排序**：

| 线 | 内容 | 理由 |
|---|---|---|
| **主线：7.5 前端规划页** | 见下方拆解 | 后端能力攒了三层没入口；7.3.5 的交互闭环只能靠 UI 完成 |
| **后台并行：7.4 放量** | 斗破余 1471 章分批跑 `--stage all`（30 章 ≈ 232s ≈ ¥0.1，全程约 3~4h 纯跑批，分多次挂机）；跑完做跨书聚类验证 + 检索/选角效果重测 + **DORMANT_GAP=30 阈值校准**（7.3/7.3.5 两个"拍的"参数等这个数据） | 跑批不占开发时间；7.1.3 结论"先放量让真实成本暴露"；连续性阈值标定也依赖它 |
| **最后：7.6 批量生成器 + FC** | 排序不变 | 依赖 7.5 的规划页做批量操作台 |

**7.5 拆解（按依赖顺序，每步可独立验收）**：

1. **入口与骨架**（先解决"零入口"）：读 `router/index.js` 现状 → 侧栏 `globalEntries` 或顶部 `meta.tab` 加"篇规划"入口 → 空页面可达。⚠️ 收尾铁律：路由 + 侧栏/tab + `router.push` 三者必有其一可达路径。
2. **计划视图**：结构化表格渲染计划行（no/beat/summary/new_chars/recall_chars/template_ref/target_words/hook）；行级「AI 改这行」（`refine_line`）；拍板按钮。**`new_chars_dropped`（超限额剔除）要在 UI 提示**，不能静默。
3. **casting 面板**：槽位 → 角色 → 分数/来源（auto/manual）→ 手改（PUT）→ 重算（recompute）→ 出场校验刷新（refresh-appearances）。分数展示标注 `MIN_SCORE=0.58` 阈值线。
4. **引入单面板（7.3.5）**：pending 列表 → 确认建卡（confirm，可带 attrs）/ 忽略（dismiss）；unmatched_slots 高亮引导作者关联槽位；`first_appearance` 展示**必须标注"篇内行号"语义**（与全局章号两套编号，混用会误导）。
5. **连续性提示（7.3.5）**：carryover 差集名单从 plan JSON 读出，横幅警告（交代离场/安排出场/忽略三选一提示，处置动作可为后续小迭代）；`needs_reentry_note` 的选角行展示回归理由材料包（reentry-material）。
6. **C 档顺带**：角色状态可视化（status/last_seen/disappear_mode 列表展示 + 手改入口）——只做展示不改数据流，自动抽取仍等真实长篇。

**验收门槛**：演示项目走通"生成计划 → 看到选角 → 确认一个新角色 → 看到交接警告"全流程，全程不碰 API 工具。

**实施进度（2026-09-13 当天）**：

- **步骤 1~5 一次到位**（`views/PlanView.vue` ~700 行 + `api/plan.js` + `api/casting.js` + 路由 + 侧栏入口）：
  选篇 → 生成 → 表格行内编辑/「AI 改这行」/拍板 → 选角卡片（0.58 阈值线、unmatched 高亮、手改）→
  引入单（绑定/确认建卡/忽略/恢复）→ carryover 横幅 + 回归材料折叠面板。`vite build` 通过。
- **配套后端修复**（7.3.5 遗留 bug，前端接数据时暴露）：`save_lines`/`refine_line` 重写 plan JSON
  会把 `carryover`/`reentry_materials` **洗掉**（作者一改行连续性数据就蒸发）→ 重写时保留；
  `get_plan` 此前不吐这两个字段 → 补齐。新增 1 单测钉住（mock LLM），全量 **346 全绿**。
- **待办**：真机交互验证（需重启后端 + 前端 5173）；步骤 6（C 档角色状态展示）待做。


#### 7.1 模板库

**两层资产**：
- `plot_templates`：`scale = arc(篇级 8~15 章) | segment(情节段 1~3 章，乐高件)`；核心结构
  `structure = {phases: [{phase, beats: [{beat, variants: [{src, how}]}]}]}` ——
  **beat × variants 是灵魂**（卡文时查的是"这个节拍别人还有哪几种走法"，`src` 溯源到哪本书）；
  `genre_tags` 做跨题材同构匹配（学院大比 ≈ 商业竞标 ≈ 武林大会）；`pitfalls` / `rhythm` / `source_stats`。
- `chapter_summaries`：入库管线中间产物（书名/章号/百字概括/段归属/情节标签），本身是可回溯资产。

**检索三层**（回答"原文存还是向量查"：**向量负责找，结构化 JSON 负责用，原文永不入库**）：
1. `genre_tags` / `scale` 标签过滤（SQLite，零成本）；
2. **bge-m3 向量检索，chunk 粒度 = beat**（不是整篇模板）—— 篇规划查模板级目录，**卡文时精确命中当前 beat 的 variants**；
   混合语义（"学院大比+秘境寻宝"）天然落在两簇之间、两边都召回；多关键词走 multi-query + RRF（A 线现成逻辑）；
3. LLM 扩查（可选）：口述太模糊时先拆成 2~3 个候选查询再各查一路融合。

**消费两条路**：
- **路径 A · 嵌入式（MVP 首选）**：top-2 模板的结构 JSON（2~4k tokens）直接拼进规划调用，一次出章计划；
  简单、可调试、无闭环、模板前缀缓存友好；
- **路径 B · 工具式（Phase 6 落地后升级）**：模板目录常驻 prompt（几十 token/个），AI 调
  `retrieve_templates(query)` 拉详情 —— 库大了不爆 prompt，步数上限 3，**可降级回路径 A**。

**入库向导（项目内页面，六步）**：切章 → 逐章概括（LLM，落库断点续跑）→ 情节段切分 → 段分类打标 →
**人工合并（AI 候选组 + 用户点确认）** → 凝练入库 + 向量化。
向量化进 `vector_chunks`（`source_type='plot_template'`）→ **A 线 Hybrid 检索直接复用，检索代码零新增**。

#### 7.2 篇规划流

- 新表 `article_plans`：article 绑定、`template_ids`、`plan` JSON（行 = 章号/beat/summary/new_chars/
  recall_chars/target_words/hook）、`origin = template|free|ai_draft`、`raw_ai` 留档（4.3 反馈对比用）、`status`。
- 规划上下文**全部复用现成字段**：卷大纲、上篇结尾（stage_summaries）、活跃角色（characters + memories）、
  未回收伏笔、设定 —— 不新建上下文系统。
- **防幻觉双闸**：`recall_chars` 后端校验 `characters` 表存在性（查无此人即剔除）；`new_chars` 只进
  `planned_chars`，拍板后走既有的待确认实体机制。
- **局部修改（结构化表格，不是一坨文本）**：行级操作 = 改 / 删 / 插 / **换走法**（该 beat 的其他 variants）/
  **AI 只改一行**（`refine-line {line_no, instruction}`，其余行原样）—— 不是全局重新生成。
- **未找到参考，三种情况都不硬凑**：① 检索为空 → 自由规划模式，或口述升格为 `ai_draft` 临时模板（**可反哺模板库**）；
  ② 模板与本书设定冲突 → LLM 标注冲突 beat，前端标黄逐行决定；③ 本书上下文不足 → 降级跳过召回建议。
- **生成联动（最后一米，2.1 伏笔回注同款模式）**：`build_chapter_messages` 新增 `layer_chapter_plan` ——
  注入本章 beat / 目标字数 / 结尾钩子 / 可引角色 + 上一章完成情况。

#### 7.3 批量生成器

- 按 plan 连写 M ≤ 3 章；每章 = 生成 → 落库 → **同步摄取**（连写必须等摄取完成，下一章上下文才含它；
  与现行异步 `_background_ingest` 的关键差异）。
- `batch_progress` SSE 事件（第几章/共几章）+「停止」按钮；`done_up_to=N` 断点恢复，已落章节完整可用。
- 连写时同会话前缀缓存命中率高；打斗/博弈类在 plan 里拆成 2 个 beat，连写共享"场"级约束 ——
  衔接质量优于逐章手动点。

#### 7.4 FC 闭环（第一个真实场景）

- **单工具** `retrieve_templates(query, scale?, tags?)`，不做万能工具箱。
- **仅篇规划会话启用** —— 章节生成继续用确定性 top-up（那条链要的是稳定不是自主性）。
- 步数上限 3，超限降级直出计划；不支持 tool_calls 的模型自动走 7.1 路径 A。
- SSE：`tool_call`（"正在检索模板库…"）→ `tool_result`（"找到：学院大比(12本)、秘境寻宝(8本)…"）。

**2026-09-12 增补（两条）**：
- **边界**：FC 只用于**步数不确定的探索型取数**，确定性取数一律预取 —— 完整判据见 §Phase 6「🔴 边界约束」。
  据此，7.3.5 的**回归理由材料包不走 FC**（三步全确定），仅在其缺席期超窗口、确需多轮补取时才考虑。
- **候选第二工具**（不是现在做）：`retrieve_reentry_context(character, from_chapter, to_chapter)` ——
  仅当①缺席期材料超窗口且②后端两级预取效果不足时才启用。**默认不启用。**

**前置条件（2026-09-12 核实）**：规划层用的是 **DeepSeek**（`plan_crud` → `plot_import._ds_post`，关思考），
**不是本地 4b** → 模型侧支持原生 `tool_calls`，**FC 在规划链路上可行**。
但适配器当前 `chat()` **只返回 `str`，不暴露 `tool_calls`**，`reference_selector.ToolCallSelection`
**仍是抛 `NotImplementedError` 的占位**（05 §11.1）→ 需先补 `chat_with_tools()`。

**实施顺序**：7.1（**MVP 门槛：20 本灌完后，"学院大比+秘境寻宝"类混合口述能召回正确模板族**）
→ 7.2 → 7.3（可与 7.2 并行）→ 7.4。
**风险排序（诚实记录）**：模板数据质量 > 格式设计 —— 种子期只用完本/口碑书，第一批模板用户亲自过目；
格式先跑再迭代，不过度设计。

#### 7.1 试点结果（2026-09-11，修真四万年前 30 章 · Qwen/Qwen3-8B）

**跑通**：管线服务 `plot_import.py`（直连硅基流动，`enable_thinking=false`，串行 2s 间隔 + 429 指数退避）
+ 单测 11 用例（全 mock）。30 章概括 **0 失败**，19 个情节段 + 19 个标签全部产出，总耗时 29 分钟。

**质量好的**：章概括信息密度高（人物/地点/物品/数字全齐，如"灵根开发度 35%→58%""十一万元陪练合同"）；
段概括能把 2~6 章串成起因→发展→结果；标签大体准确（追逃猎杀/升级突破/模拟高考/特训指导…）。

**试点暴露的三个真问题（下轮修）**：
1. **段切太碎**：19 段/30 章（平均 1.6 章/段，单章成段过半）—— 修复：切分 prompt 强化"优先 3~6 章/段" +
   后处理合并（相邻且 embedding 相似的段自动合并）；
2. **标签有噪声**：赫连烈冲突系列 6 段全标"追逃猎杀"（实为冲突对峙）—— 修复：固定标签菜单 + 复用前次标签优先；
3. **跨批次段被切断**（段 14/15 实为一个"军道杀狼特训"故事）—— MVP 预期内，全局段边界下轮做。

#### 7.1 弧归并层落地（2026-09-11 晚，DeepSeek V4.1 Flash）

**背景**：用户察觉"切出来的段不像完整篇章"→ 诊断确认是**粒度错位** —— 段（beat）是"事件粒度"，
模板需要的单元是**故事弧**（arc，一个完整套路 = 一个爽点周期，8~15 章）。缺的就是这一层归并。

**实现**：`merge_arcs()` —— 读该书的段序列 → DeepSeek 归并 → 写回 `chapter_summaries.arc_no/arc_name/arc_summary`；
报告升级为**三级结构**（弧 → 段 → 章）。CLI `--stage arc`（`all` 已含）。

**模型调参（实测踩坑）**：`deepseek-flash` **默认思考模式开着**，2000 token 预算会被 reasoning 全部吃光、
content 返回空且耗时 10.7s；加 `thinking={"type":"disabled"}` 后 **1.7s** 拿到完整 JSON。归并任务不需要思考。

**真机验证（修真四万年前 30 章）**：19 个段 → **5 个故事弧**（covered=19，leftover=0），耗时 **5~6 秒**。
归并质量达标 —— 例如**弧 2「晶脑维修与夺舍觉醒」（8~12 章）**把原本切碎的三个段
（维修晶脑 / 幻梦百炼宗 / 反夺舍）合成了一条完整的金手指觉醒弧，正是"粒度错位"问题的直接修复。

**Key 存放**：`app_configs.llm.deepseek_key`（**只进数据库，不进代码不进 git**；已全仓库扫描确认无泄漏）。

**下一步（7.1 收尾）**：20 本放量 → 凝练模板（人工合并：AI 出候选弧 + 用户确认）→ 7.2 篇规划流。

#### 7.1 模板凝练落地（2026-09-11 晚，弧 → 模板）

**新模块 `services/plot_distill.py`**（步骤 5）：
- `cluster_arcs`：用 bge-m3 向量算弧概括相似度聚类（阈值 0.80）→ **AI 候选组**；
  向量不可用时退化为"每弧一组"（不硬报错）；
- `distill_template`：一组弧 → DeepSeek 凝练成模板结构（`phase → beat → variants[各书走法]`）+ logline/tags/pitfalls/rhythm；
  含**重试一次**（LLM 偶发格式问题，实测救回 1/5 组）；
- `distill_all`：聚类 + 逐组凝练 + 入库（`status="draft"`）；`replace_drafts=True` 重建时清旧 draft（**reviewed 不动**）。

**人工确认原则落地**：AI 产出一律 draft；人工审核后改 `status="reviewed"`。当前审核入口是 Markdown 报告 + `/plot-templates` API，交互 UI 待做。

**真机结果（修真四万年 5 弧 → 5 个种子模板）**：结构质量达标，例如「夺舍觉醒」：
4 个 phase（开局委托 / 异象触发 / 梦境历练 / 夺舍反转）× 10 个 beat，每个 beat 带 variants（`src` 溯源到书名 + 具体处理），
pitfalls 4 条都很实用（"夺舍反转缺乏前期伏笔，显得突兀机械降神"）。

**发现并修掉一个检索排序缺陷（重要）**：只索引 beat 块时，块的语义被"具体情节"主导，
模板名/logline/标签（如"金手指觉醒"）权重被稀释 → 模糊口述"主角觉醒金手指"把「夺舍觉醒」**排到最后**。
修复：**新增模板级块**（`name + logline + 标签 + 各 beat 标题`）与 beat 块一起索引（`template_chunks`），
同样 query 后排序升到**第一**。

> ✅ **已验证（2026-09-11 深夜，30 个模板）**：6 组模糊口述查询 **Top1 命中率 6/6** —— 模板级块 +
> 30 模板规模下区分度足够。详见下节。

#### 7.1 端到端贯通与门槛检验（2026-09-11 深夜）

**OpenCode 首跑（斗破 1~200 章）**：概括 200 章 ✓ / 切段 **84 段** ✓ / 分类 ✓；
**arc 归并失败**（`[arc] arcs:0`，JSON 解析失败）。

**根因（我的参数失误）**：`merge_arcs` 的 `max_tokens=3000` —— 84 段归并的输出约 6000~12000 tokens，**被截断**。
**修复**：`max_tokens` → **16000**；弧概括要求 150~250 → **80~150 字**；**分批归并**（`chunk=120` 段/批，弧号全局递增）；
失败时返回 raw 片段（便于诊断，而不是只丢一句"解析失败"）。
**重跑**：84 段 → **28 弧**（covered=84 / leftover=0，19 秒）。

**跨弧聚类 + 凝练**（33 弧 → **30 个模板**）：「苦修拍卖突破」聚 3 弧、「家族测试逆袭」聚 2 弧
（**同书内聚类成功**；跨书未聚成 —— 斗破是玄幻升级流、修真四万年是设定流，弧本身差异大，
属真实结果而非缺陷）。凝练的**重试机制**救回 1 个坏输出。

**★ MVP 门槛检验：通过**（6 组模糊口述的 Top1）：

| 查询 | Top1 命中 |
|---|---|
| 主角觉醒金手指 | 夺舍梦境觉醒 |
| 学院大比擂台赛 | 学院招生试炼 |
| 退婚打脸逆袭 | 退婚逆袭 |
| 秘境寻宝夺机缘 | 秘境盗宝 |
| 拍卖会大出风头 | 拍卖会扬名 |
| 被强者追杀后反杀 | 追杀反杀突破 |

**仍未做**：跨书聚类的规模验证（需 ≥5 本书）、归并/凝练的确认 UI（当前靠 Markdown 报告）。

#### 7.2 篇规划流落地（2026-09-11 深夜，端到端验证通过）

**新表 `article_plans`**（一个篇一条当前计划）：
`plan["lines"]` 每行一章 `{no, beat, summary, new_chars, recall_chars, target_words, hook, template_ref}`；
`origin=template|free|ai_draft`；`raw_ai` 留档（供 4.3 反馈对比）；`status=draft|confirmed`。

**生成**（`plan_crud.generate_plan`，DeepSeek 关思考）：
模板检索（top-2，检索不到 → 自由规划 `origin=free`，**不硬凑**）+ 本书上下文
（卷概要 / 角色 / 伏笔 / 前情结尾）+ 作者口述 → 逐章计划。
**防幻觉双闸**：召回角色后端校验（查无此人剔除并报告 `unknown_chars`）；新角色只进计划、拍板后走待确认实体。

**行级局部修改**（不用重新生成整份）：
- 表格直接改：`PUT …/plan`（`save_lines`）
- **AI 只改一行**：`POST …/plan/refine-line {line_no, instruction}` —— 其余行原样
- **拍板**：`POST …/plan/confirm`（draft → confirmed）

**生成联动（最后一米）**：`layers.layer_chapter_plan`（**P_CRITICAL 级，永不裁剪**）——
把作者确认的「本章任务卡」注入生成上下文：节拍 / 要点 / 钩子 / 字数 / 新角色 / 召回角色 + 前后章计划。

**★ 踩坑（重要，已修）**：SQLAlchemy JSON 列的**共享引用污染** ——
`lines = plan.plan["lines"]` 拿到的是**同一个列表对象**，修改它会把 SQLAlchemy 记住的旧值一起"污染"，
赋新值时 `new == old` → **UPDATE 被跳过**（实测：refine 返回正确但库纹丝不动，DeepSeek 白调）。
修复：取 lines 时 `copy.deepcopy`。**方法论：凡"读 JSON 列 → 改 → 写回"，取值时必须先深拷贝。**

**端到端验证（真实 DeepSeek，临时项目已清理）**：6 章退婚流计划生成 ✓
（模板检索命中「退婚逆袭」、每章 beat/钩子/字数齐全、防幻觉剔除 3 个幻觉角色）
→ AI 改第 2 行 ✓（其余行不动）→ 拍板 ✓ → **layer_chapter_plan 产出完整任务卡** ✓。

**API**（5 个，路由 160 / paths 96）：generate / GET / PUT / refine-line / confirm。
**待做**：前端规划页（结构化表格 + 行级编辑 + 拍板按钮）—— 当前可先用 API 驱动。

#### 7.2.1 匿名化归一（2026-09-11 深夜，用户方案：入库即代称世界）

**动机**：用户实测发现计划照抄斗破开局（云岚宗原名照搬）→ 根因是概括/模板带源书专名。
**用户方案（拍板）**：入库概括时就把专名替换为代称 —— 治本（反抄袭从"堵"变"无需"）。

**代称体系**（含三处修正）：
- 主角 → `主角`；配角/组织 → `友·配角1` / `敌·宗门2`（**敌友前缀 + 数字编号**；
  修正 1：不用 A/a 大小写 —— LLM 笔误且检查器无法自证）
- 境界 → `境界1~境界N` **绝对阶梯**（修正 2：不用"相对主角"的 C/D/B —— 主角升级后锚点漂移）
- 每个代称带 `role_desc` 槽位功能说明 → 计划生成时**选角**到本书角色
  （修正 3：解决「模板 A 的男配10 与模板 B 的男配13 聚合时角色混乱」——
  模板代称是来源书槽位，计划里一律映射回本书真实角色）

**实现**：新表 `book_aliases`（双向表：正向匿名 / 反向还原）+ `services/anonymizer.py`
（cast 抽取 → 代称分配 → 长名优先正则替换）+ 概括原文备份 `*_raw` 列（可回滚）+ CLI `--stage anonymize`。

**踩坑**：①LLM 自报境界顺序不可靠 → 按**首现章号**排序；②对已替换文本再抽取 →
「一星境界2」链条污染 → 抽取读 `summary_raw` + 过滤子级条目（含数字段/星阶）。

**真机**：斗破 152 条映射、修真 52 条（秒级）；**重凝练后 29 个模板，16 个源书专名命中 0** ✓。
计划生成同步加「选角规则」。单测 9 例；全量全绿。

**速度注记**：30 章 17 分钟（~34s/章）→ 755 章全量约 7 小时。放量前需做**并发优化**（3~5 并发 + 限速器改为令牌桶）。

#### 7.1 速度优化实测（2026-09-11 晚，斗破苍穹）

**动机**：串行逐章跑斗破 1671 章需 14~16 小时，必须优化。

**实测对比（各 9 章 / 30 章，`interval=2s`）**：

| 配置 | 每章耗时 | 1671 章推算 | 失败 |
|---|---|---|---|
| A 逐章串行（原） | 30.2s | ~14 小时 | 0 |
| B **批量 3 章**/请求 | 5.1s | ~2.4 小时 | 0 |
| C 批量 3 + 并发 3 | **2.2~2.5s** | **~70 分钟** | 0 |

（C 配置三轮 30 章复测：2.51s/章、`failed=0`；其中漏章补跑已含在内，见下）

**结论：批量是主杠杆（5.9×），并发再叠 2.4×，合计提速 ~11 倍**。原因：单章那 30s 里**绝大部分是固定开销**
（排队 / 首 token / prompt 处理），输出 150 字还是 450 字对时长影响很小 —— 一次问 3 章等于把固定开销摊薄到 1/3。

**实现要点**（`import_chapters_batch`）：
- 线程模型：**LLM 调用在工作线程（纯 IO），DB 写入全部回主线程串行** —— SQLAlchemy Session 非线程安全，
  这样既拿到并发又避开多线程写 SQLite；Key 在主线程预取后传入 `_sf_post`（工作线程不碰 session）；
- `RateLimiter` **加锁**（多线程共享，否则多个请求同时穿过判定 → 打爆限流）；
- **漏章补跑（两级）**：实测批量模式下 8B 偶尔漏掉批内某章（30 章漏 1~4 章）→ ① 把漏章**重新凑批再跑一轮批量**
  （3 章批量 ~15s，比单章 3×30s 省得多）；② 仍漏的才用**单章 prompt 兜底**。
  30 章复测（漏 4 章）`failed=0`、2.51s/章 —— 单章补跑会让耗时涨到 4.17s/章，两级补跑把它压回 2.51s；
- **概括有效性校验** `_valid_summary`：≥15 字且无 JSON 残片特征（单测暴露出补跑路径会把 "这不是 JSON" 原样入库）。

**未做**：令牌桶（当前用"加锁的固定间隔"，实测未触发限流，够用；若要更高并发再上）。

---

#### 7.1.1 入库管线补全（2026-09-12 ✅ 完成 —— 真机端到端验收通过）

**背景**：用户将放**多个 AI 并行灌数据**。深查入库链路后发现 **4 个洞**，其中第 1 个会直接打破已通过的门槛。

**已落地（2026-09-12）**：
- `--stage all` 现在 = 概括 → **匿名化** → 段 → 分类 → 弧 → **`verify` 自检** → 报告（顺序改动见下"洞 1"）；
- 新增 `--stage verify`：**纯 SQL 零调用**的专名残留 + 覆盖率自检（把门槛 2 变成可回归）；
- 新增 `--stage full` = `all` + distill（单点用；**专名残留不为 0 时拒绝凝练**）；
- 凝练加 cast（B 方案，见 7.3）；`core/database.py` 加 WAL + `busy_timeout` + `synchronous=NORMAL`。

**★ 真机端到端验收（2026-09-12）**：
- `--stage all`（道诡异仙 1~30 章，424s）：概括 30 章 0 失败 → 匿名化 38 映射触及 30/30 → 段 24 →
  分类 24/24 → 弧 8（covered 24 / leftover 0）→ 自检通过 → 报告。
- `--stage verify`（3 本书）：斗破 200 / 修真 30 / 道诡异仙 30 → 覆盖 100%、**人物势力类专名残留 0**。
- `--stage distill`（3 本书 41 弧 → **37 模板**，193s）：**37/37 含 cast**，每模板 3~6 槽位，
  slot 为功能名、`srcs` 溯源正确、**同功能位跨代称合并生效**。
- 单测 **297 全绿**。

**验收中发现并修掉的 2 个真 bug（都是我自己引入的）**：
1. **`_book_cast` 的映射只取了 `CAST_KINDS`** → `role_desc` 里 item/place/realm 类专名全部漏网，
   实测 18 处（清风观 / 紫晶源 / 斗之气 / 魔兽山脉 / 炼药师公会…）带进了模板 cast。
   修复：映射改用**全 kind**；`mapping_strong` 再剔 `item`（该类把「丹药」这类普通名词也当专名，
   替换反而污染向量语义）。
2. **LLM 写的 `desc` 没有兜底匿名化** → 模型会自己带出源书专名（DeepSeek 的先验知识）。
   修复：`_normalize_cast` 里对 desc 再过一遍匿名化（确定性兜底，不只靠 prompt）。
   结果：cast 中真专名残留 **18 → 0**。

**顺带修**：`verify` 的判据原先不分类型 → 把 item 类普通名词误判为失败（道诡异仙 21 处「丹药」），
现分**硬判据**（protagonist/role/sect/family/force，判失败）与**软提示**（item/place，只报告）。

**仍未做**：WAL 是否真生效（需重启后端后查 PRAGMA）；**跨书聚类仍未成功**
（3 本书 41 弧只聚成 37 组 —— 流派差异大，属真实结果，需 7.4 放量后再看）。

**实测现状**（脚本 `backend/scripts/import_novel.py` → `services/plot_import.py`）：

| 项 | 实测 |
|---|---|
| 模型分工 | 概括/切段/打标 = **硅基流动 `Qwen/Qwen3-8B`**（`enable_thinking=false`）；**弧归并 = DeepSeek `deepseek-flash`**（`thinking={"type":"disabled"}`） |
| `--stage all` 内容 | summarize + segment + label + arc + 报告 |
| **是否含 anonymize** | ❌ **不含**（`plot_import.py` 全文无 anonymizer 引用，已 grep 确认）。现有 204 条映射是用户**事后单独跑** `--stage anonymize` 补的 |
| 是否含 distill | ❌ 不含（需单独 `--stage distill`） |
| 入库格式 | **当前 7.1 版**（章概括=纯文本；段=`{segments:[{chapters,summary}]}`；弧=`{arcs:[{segments,name,summary}]}`）—— 与 7.3 的差距**只在"无 cast"** |
| 是否写 `characters` 表 | ❌ 不写。仅在 anonymize 阶段把「代称 + role_desc」写入 `book_aliases` |
| 是否有角色向量 | ❌ 无。`vector_chunks` 只有 `plot_template`(665) + `ref_doc`(133) |

> ⚠️ **概念区分（防止后续混淆）**：**模板侧槽位**来自源书，存 `book_aliases`，用于凝练进模板 cast；
> **本书角色池**是作者自己小说的角色，存 `characters`，用于 7.3 选角。
> **素材入库绝不该往 `characters` 写** —— 那是作品自己的角色库，会被 20 本小说的人物灌爆。

**四个必须堵的洞**：

1. **`--stage all` 不含 anonymize** → 并行 AI 按现有投递单跑出的书 = 概括/段/弧齐全但**未匿名**；
   接一次 `distill` 就凝练出**带源书专名的模板**，**直接打破门槛 2**（"16 个源书专名命中 0"）。
   → 改：`all` = summarize → **anonymize** → segment → label → arc（匿名化紧随概括，下游段/弧都在干净文本上生成）。
2. **`distill_all` 默认 `replace_drafts=True` 会清空所有 draft 再重建**（`plot_distill.py:234`）
   → **并行跑 distill 必然互相清空**。→ 定规矩：**并行 AI 只跑到 anonymize 为止；distill 由单点串行跑**
   （新增 `--stage full` = all + distill，仅供单点使用）。
3. **数据库未开 WAL / `busy_timeout`**（`database.py:53-58` 只设了 `check_same_thread=False`）
   → 并行多本书写库会 `database is locked`。→ 加 `journal_mode=WAL` + `busy_timeout`（改完需重启后端）。
4. **`RateLimiter` 是进程内的**（`plot_import.py:60`）→ N 个 AI 并行 = **N 倍请求率**
   （投递单铁律 2/3 禁并行正为此外）→ 并行时每个 AI 降并发，并限定书数上限。

**改动清单**：
- `plot_import.py` / `import_novel.py`：`all` 纳入 anonymize；新增 `--stage full`；帮助与阶段说明更新。
- `plot_distill.py`：`_distill_prompt` + `distill_template` 加 cast（B 方案，见 7.3）。
- `core/database.py`：WAL + busy_timeout。
- **`.workbuddy/tasks/2026-09-11-模板导入-OpenCode.md` 必须同步改**（命令 / 铁律 / 验收），
  否则并行 AI 照老单子跑，第 1 个洞照样发生。
- 每本书验收加一项：**源书专名命中 0 抽检**（门槛 2 的回归测试）。

**推进顺序（用户指定）**：改完 → **用户先跑几十章验收** → 再放并行 AI 灌数据。

##### 7.1.2 成本可观测 + prompt 瘦身（2026-09-13 ✅ 完成 —— 真机验证通过）

**动机**：用户发现「昨晚 30 章就 0.24 元」，且 DeepSeek 缓存命中率偏低
（实测 **27,768 命中 / 67,144 未命中 / 43,762 输出，命中率 29.3%**，官方公开均值约 34.5%）。

**为什么贵（诊断结论）**：三档单价差极大 —— 缓存命中输入 `$0.003/M`、
未命中输入 `$0.15/M`（**×50**）、输出 `$0.60/M`（**×200**）。
所以**优化目标不是"提高命中率"，而是"砍总输入 + 砍输出"**。

**改动（两项，均已完成并真机验证）**：

1. **离线管线用量记账**（此前 `llm_usage_logs` **0 行**，成本完全是黑箱）
   - `_chat_post` 新增 `on_usage` 回调（纯 HTTP 层不持 Session，保住"Session 非线程安全"约定）；
     `make_usage_cb(scene)` 在**调用发生的那个线程**里现开现关独立 Session → 并发路径天然安全。
   - `llm_usage_logs` 新增 `cache_hit_tokens` / `cache_miss_tokens` 两列（`_auto_migrate` 自动补）。
   - `_extract_cache` 归一三种厂商格式：通用 / DeepSeek `prompt_cache_hit_tokens` /
     OpenAI `prompt_tokens_details.cached_tokens`。**未知归一为 (0,0) 而非 (0, prompt_total)** ——
     后者会把"厂商没回传"伪装成"全部未命中"，把统计打成一片红、误导优化方向。
   - **14 个调用点全部接上**：概括 / 段切分 / 分类 / 弧归并 / cast 抽取 / 凝练 / 规划 / 改行。
   - 真机实测：连调 3 次，命中 `0 → 256 → 256`（命中率 55%）；
     `_extract_cache` 对用户真实数据算出 **29.26%**，与 27,768/94,912 吻合。

2. **`_plan_prompt` 模板注入瘦身**
   - 模板只送骨架（`name` / `logline` / `rhythm` + 各 phase 的 **beat 名**），
     **剥掉 `variants` 明细与 `pitfalls`**。理由：一个模板 `structure` 实测 4062~6620 字符，
     variants 占绝大部分，top-2 检索就是 8000+ 字符/次；而规划只需要知道"有哪些节拍可选"。
   - **保留 beat 名是刻意的** —— 模型要靠节拍名填 `template_ref`。
   - 实测：`_plan_prompt` **2523 → 1167 字符（−53.7%）**，token 估算 1682 → 778。

**单测**：全量 **297 例全绿**。**临时验证文件已全部清理，测试用量记录已清空。**

**尚未做（已记录，待后续）**：
- `_arc_prompt` 的 `chunk` 合并（有截断风险，需同步提 `max_tokens`）
- 固定指令抽成 `system` role（预期提升前缀可缓存性，**未实测**）
- `merge_arcs` / `distill_all` 的幂等跳过（避免重跑重复烧钱）
- 前端观测页补缓存命中率展示

##### 7.1.3 凡人修仙传真机实测 —— 成本基线（2026-09-13 ✅ 跑通 + 数据推翻原优化假设）

**跑批**：凡人修仙传 1~30 章 `--stage all --batch-summarize 3 --concurrency 2`，**232s**。
29 章概括 0 失败 → 41 映射匿名化触及 29/29 → 段 27 → 分类 27/27 → 弧 7
（covered 27 / leftover 0）→ **人物/势力类专名残留 0**。

**首次拿到真实成本分布**（44 条记账记录，此前是黑箱）：

| 场景 | 次数 | 输入 | 命中 | 未命中 | 输出 | 命中率 | 成本占比 |
|---|---|---|---|---|---|---|---|
| **sf_summarize** | 12 | 48,253 | 384 | 47,869 | 2,763 | 0.8% | **65.9%** |
| sf_segment | 3 | 3,162 | 0 | 3,162 | 2,787 | 0% | 16.0% |
| ds_cast_extract | 1 | 2,484 | 0 | 2,484 | 1,127 | 0% | 7.8% |
| ds_arc | 1 | 2,545 | 0 | 2,545 | 749 | 0% | 6.2% |
| sf_label | 27 | 4,284 | 832 | 3,452 | 62 | 19.4% | 4.2% |
| **合计** | 44 | 60,728 | 1,216 | 59,512 | 7,488 | **2.0%** | — |

模型分布：**Qwen3-8B 42 次 / DeepSeek 2 次**（折算 ≈ ¥0.095）。

🔴 **三条推翻性结论**（修正此前凭用户汇总数所做的判断）：

1. **输入是大头（66.5%），输出只占 33.5%** —— 此前推测"输出最贵"（单价 ×200）在**单价上对**，
   但**总量上输入压倒**：输入 60,728 vs 输出 7,488（**8:1**），乘上 50 倍价差后输入仍占 2/3。
   → **优化要盯输入总量，不是盯输出**。
2. **缓存优化天花板极低**：输入的 **98% 是未命中**（59,512/60,728），因为送的是
   **每次唯一的正文/概括**，物理上无法缓存；固定指令段最多 405 token/次，44 次全命中
   也只能省 **19.5%**。→ **"抽 system role"这类缓存优化收益有限，且未实测**。
3. **截断上限 6000 字无浪费**：凡人修仙传单章实际 1700~2650 字，**0 章触发截断**，
   降到 3000 也无收益（除非冒丢章尾转折的风险）。→ **"减少送进去的字数"此路不通**。

**硅基流动缓存机制（实测）**：Qwen3-8B **不在**其缓存计费模型列表（仅 Qwen3.5/3.6 有缓存价），
但仍回传 `prompt_cache_hit_tokens`，且**命中值恒为 64 或 128**（= 1~2 个 64-token 块）
→ 平台侧自动前缀缓存，**64 块对齐 + 要求完全相同字节前缀**。

**修正后的优化优先级**：

| 原计划 | 实测评估 |
|---|---|
| `_arc_prompt` chunk 合并 | ⚪ 收益小（输入总量不变，只减请求数） |
| 固定指令抽 `system` role | ⚪ **天花板 19.5%，且未实测** |
| `merge_arcs` / `distill_all` 幂等跳过 | 🟢 **仍值得做**（避免重跑重复烧钱，与命中率无关） |
| 前端观测页补缓存命中率 | 🟡 数据已有，展示仍值得 |

**下一步**：先放量（7.4）让 **DeepSeek 侧（distill 阶段）的真实成本**暴露出来 ——
本次 DeepSeek 只占 2 次调用，样本不足以判断 7.3 之后的成本压力。

---

#### 7.3 角色向量选角（2026-09-12 规划 → ✅ 2026-09-13 已实现 —— 用户方案）

**动机**：计划生成的"选角"目前靠 LLM 在 prompt 里自行分配 —— 跨模板聚合时
（模板 A 的友·配角10 与模板 B 的友·配角13 是两个世界的角色），代称对 AI 无意义，容易混乱；
且选角结果不可解释（为什么是药老？）。

**方案（用户提出）**：把模板角色从「代称」升级为「**人格化槽位**」—— 选角变成**向量检索 + 可解释匹配**。

##### cast 结构定稿：B 方案（功能槽位 + srcs 溯源）

**不做 A 方案**（cast 直接记来源书代称）。A 的问题不在单书 —— 单书时两方案产出几乎逐字相同
（**这正是"看不出差别"的原因**：当前 29 个模板全是单书模板）。差异在**跨书聚成一组后**爆发：
斗破的 `友·配角4` 与修真的 `友·配角2` 其实是**同一个功能位**（引路人师长），但编号/措辞不同 →
cast 从 2 个功能位膨胀成 4 个槽位 → **两个槽位抢同一个角色**，即"模板 A 配角10 vs 模板 B 配角13"
的原病从"计划里"前移到"模板里"。且代称编号随重聚类漂移 → 已定 cast 失效。

```json
{"slot": "引路人师长",
 "desc": "主角的导师型角色，掌握关键资源、推动主角成长",
 "mode": "助力",
 "beats": ["异象触发", "梦境历练"],
 "srcs": [{"book": "斗破苍穹", "alias": "友·配角4"},
          {"book": "修真四万年", "alias": "友·配角2"}]}
```

- 与 `variants[{src,how}]` **同构**：`slot` 是跨书契约，`srcs` 溯源到各书实现。
- 每模板 **cast 限 3~6 个关键槽位**（不列全部代称，否则模板爆炸）。
- 校验：`srcs` 里的 alias 必须真实存在于该书 `book_aliases`，否则丢弃。

##### 六步实现顺序

1. **凝练带 cast**：`collect_arcs` 联查 `book_aliases`（`kind ∈ protagonist/role/sect/family/force`）
   把 `role_desc` 带进凝练输入；`_distill_prompt` 输出 schema 加 `cast`。
   ⚠️ **`role_desc` 必须即时匿名化**（复用 `anonymizer._replace_all`，**只在内存层替换，不改库值** ——
   `book_aliases` 要留作反向还原原料）。**实测风险（2026-09-12）**：现有 204 条 `role_desc` **直接含源书专名**
   （"…称主角为**萧炎**哥哥""**云岚宗**宗主""**萧薰儿**"）→ 原样入 cast 会**打破已通过的反抄袭门槛 2**。
2. **槽位向量库**：`source_type="plot_cast"`、`project_id=__global__`、
   **`source_id=template_id` + `chunk_idx = cast 数组下标`**（命中后直接 `structure["cast"][i]` 反查，
   不必解析 chunk 文本）。向量文本 = **功能描述 + srcs 典型实现（混合文本）** ——
   纯功能描述过于抽象，与"具体角色人设"的向量距离偏大、分数会整体偏低。
   `create`/`update`/`delete` 三个入口都要维护，**两个 source_type 都要清**（否则留孤儿向量）。
3. **计划向量选角 → 新表 `plan_castings`**：
   `id, plan_id, project_id, article_id, template_id, slot, slot_desc, slot_mode,`
   `character_id, character_name, score, source(auto|manual), needs_reentry_note`，**`UNIQUE(plan_id, slot)`**。
   - **表是唯一真相源**，plan JSON 里只放**只读展示冗余**；人工改 casting 走独立端点，**不碰 plan.json**
     （避开 7.2 已踩的 JSON 列共享引用坑）。
   - **必须登记进级联删除**（`project_crud` / `article_crud` / `volume_crud`）。
   - **打分统一走显式余弦**（两侧文本批量 embedding 后在 Python 里算），**不用 store 的 score**：
     后端生效实现是 `SqliteVecStore`，其 `score = 1/(1+L2)` **不是余弦** ——
     同一 0.7 余弦在库里显示 **≈0.56**，**若照 0.65~0.75 直接标定会过严（实际要求 cos≈0.90+）**。
4. **角色状态门**（A 档，从 7.3.5 并入 —— 见下方"为什么并入"）：候选池先按状态过滤，
   `已死`/`离场` **默认不进池**（可显式放行）。**没有它 casting 会系统性产出连续性错误。**
5. **阈值标定 + 出场校验**：标定前提见下；出场校验复用 `entity_graph` 一跳扩展，
   **只做过滤/标记，不参与打分**。
6. **平局判据（两级）**：① 历史共现次数 → ② 活跃度（`last_seen` 更近者优先）。
   **只改排序，不动打分。**

##### 三处算法决策（讨论定稿）

- **不做「出现可能性 × 相似度」的乘法**：槽位是否出现是结构决定的（节拍被采用=出现），
  乘法让两个误差相乘、量纲混乱。两步走：①选角（人设相似度）→ ②出场校验（历史出现 + 节拍需要）。
- 多候选冲突用「历史共现次数」打破平局。
- **（2026-09-12 新增）否决"活跃系数"乘法惩罚**：用户提出"长缺席角色用活跃系数惩罚"，意图正确
  （长期不出现→合理召回难度大），但**形式与上一款是同一个错误** —— 无量纲启发式标量乘到余弦上，
  说不出量纲含义、无法标定，且**当前无真实长篇数据可标定（只能拍，而拍出来的系数比拍出来的阈值更危险：
  阈值错只是漏召回，系数错是系统性扭曲排序且无人能发现）**。
  替代（效果相同且干净）：**① 硬门 = 离散状态，不参与 vs 分数低；② 平局次级判据 = 相似度接近时
  （top1−top2 < 0.03）优先选活跃度高的**。若将来真实数据证明排序确实被长缺席角色污染，
  **正确形式是"先归一化再线性加权"（`0.85·cos + 0.15·activity`，activity 归一到 [0,1]、权重可标定），不是乘法。**

##### 阈值标定前提（硬约束）

现有 `characters` 表只有 **2 个角色**（林尘/苏若雪），且 `personality`/`background`/`brief`/`talent`
**全为空字符串** —— 真实数据上**没有可向量化的角色池**，匹配效果与阈值都无从实测。
→ 标定必须用**临时测试项目 + 8 个带真实人设的角色**（覆盖师长/对手/同伴/长辈等类型），
产出标定表（正例最低分 / 负例最高分 / 分位数），**用完连同角色、向量、记忆一起删**（顺带验证级联删除）。

**验收（对应 docs/01 §4 门槛 6）**：同一配角在整篇计划中身份一致；每个选角有可解释的匹配分数与槽位说明；
无匹配的槽位明确走 `new_chars`（不硬凑）；**已死角色不出现在任何一行**。

**前置依赖**：匿名化归一（✅ 已落地，否则槽位文本带源书专名）；7.1.1（凝练加 cast）。

##### ✅ 实施记录（2026-09-13，一次一个模块全部落地，单测 324 全绿）

**六步交付**（对应上面"六步实现顺序"）：

| 步 | 交付物 | 关键实现 |
|---|---|---|
| ② 槽位向量库 | `plot_template_crud` | `SOURCE_TYPE_CAST="plot_cast"` 与模板块（`plot_template`）**分开索引、分开清理**（KNN 互不淹没）；`chunk_idx == structure["cast"] 数组下标`（命中直接反查）；向量文本 = `slot｜desc｜定位：mode｜srcs 实现` 混合文本。`index_template`/`delete` 都先清两个 source_type |
| ③ 选角 + 新表 | `casting_crud.cast_slots_for_plan` + `PlanCastingORM` | 读计划 → 模板槽位 → 状态门池 → 双侧 embed → **显式余弦** → 平局重排 → 阈值 → 一角色一槽位 → `_persist`。**`UNIQUE(plan_id, slot)`** 数据库级保证"同篇同功能位一个角色"；表是唯一真相源，`_persist` 只删 `source="auto"` 行（manual 保留）；降级路径全部返回 `reason`（无槽位/池空/embed 失败） |
| ④ 状态门 | `character_pool` + `characters` 新 6 列 | 四态 `alive/dormant/departed/dead`（`status` default alive，`_auto_migrate` 自动补列）；**dead 默认不进池**（显式 `include_dead=True` 放行，供回忆/幻象）；**departed/dormant 保留在池 + `needs_reentry_note=True`**（能演但写作端必须给回归理由——比一刀切剔除更符合"连续性"本意）；alive 且 `last_seen < current−30` 按蛰伏处理（`DORMANT_GAP=30` ⚠️ 只能拍，等 7.4 放量校准）。`last_seen_chapter`/`appearance_count` **纯派生**（扫 `chapter_memories.characters` 统计，`refresh-appearances` 端点触发），`status` 抽取 + 人工可改 |
| ⑤ 阈值标定 | 临时脚本（用完已删） | 临时项目 8 角色全量人设 × 6 槽位正负例，**真机 bge-m3 embedding、零 LLM**。结果：正例最低 **0.585**（退婚未婚妻）/ 负例最高 **0.596**（慕容霸在「豪爽同伴」，唯一过线负例），Top1 六槽位全中。**取 `MIN_SCORE=0.58`（交叠区下沿）**：保住全部已知正例（脚本建议的中点 0.5904 会卡掉 0.585 的正例，样本 6×8 太小不值得）；0.596 近邻错配有平局判据 + `set_casting` 人工改派兜底。标定表存档 `outputs/选角阈值标定.md`，临时项目级联删除验证残留 0 |
| ⑥ 出场校验 | `casting_crud.check_appearances` | 剔除死角色（改 `lines["recall_chars"]`）+ 对 departed/dormant 标记"需回归理由"。**⚠️ 时序关键：必须在计划落库前跑**（它改的是 lines dict，落库后跑不持久化）；`generate_plan` 在防幻觉闸门后调用；选角本身移到落库后 `_run_casting()`（只做选角、绝不抛异常，失败置 `casting_reason`） |
| API | `routers/casting.py`（4 端点） | `GET …/casting`（读）、`POST …/casting/recompute`（重算，manual 不覆盖）、`PUT …/casting`（手改，标 manual；**dead 拒绝**并提示先改状态）、`POST /projects/{pid}/characters/refresh-appearances`（重算派生字段） |

**验收对照（docs/01 §4 门槛 6）**：同篇同槽位唯一 ✅（UNIQUE 约束）；每个选角带分数与槽位说明 ✅；无匹配走 `new_chars` 不硬凑 ✅（低分槽位留空 + reason）；**已死角色不出现在任何一行** ✅（状态门 + 出场校验双保险）。

**顺带修复的 2 个存量 bug**：

1. **`distill_all(replace_drafts=True)` 批删 draft 不清向量** —— `db.query(...).delete()` 绕过 ORM 钩子，
   `vector_chunks` 留孤儿块，**每重跑一次积一批**。修复：批量删前先逐 id `remove_source` 清两个 source_type
   （`init_db` 只加列不加约束，SQLite 外键默认关靠不住）。
2. **`_book_context` 裸 `limit(40)` 无排序** —— 死人平等进规划上下文。修复：
   `filter(status != "dead")` + `order_by(last_seen_chapter.desc().nullslast(), created_at.asc())`。

**测试**：`test_casting.py` 27 例（索引/余弦/状态门/平局/流程/级联/标定 7 组）；
级联删除三入口（篇/卷/项目）全接入 `PlanCastingORM`（`project_crud._RELATED` + `article_crud`/`volume_crud` 显式删，因 SQLite 外键默认关 CASCADE 静默失效）。

---

#### 7.3.5 角色连续性（2026-09-12 规划 → ✅ 2026-09-13 B 档已实现 —— 用户提出）

##### 为什么单独立项：四问实测，三处零机制、一处有数据没逻辑

| 用户担心 | 实测 | 证据 |
|---|---|---|
| AI 需要新角色 → 合理引入 | ❌ **不存在**。`planned_chars` 全仓库只出现 **2 次且都是注释** | `routers/plans.py:12`、`services/plan_crud.py:10` —— 无列、无代码、无端点；"拍板后走待确认实体"是**空头承诺** |
| 角色已死 → 被复活 | ❌ **无法表示**。`characters` 表**没有 status 列** | `models/orm.py:31-50`（隔壁 `factions` 有，`orm.py:86`） |
| 几十章没出现却被匹配到 | 🟡 **数据在、没人读**。`chapter_memories.characters` 每章落库 | `orm.py:411`；但全仓库唯一读取点是 `memory_crud.py:207` 的序列化 —— **无任何逻辑算"最近出场"** |
| 上篇在场 → 本篇莫名消失 | ❌ **完全没人管**。篇间无交接 | `layers.layer_chapter_plan` 注入 beat/要点/钩子/字数/前后章，**无角色状态**（`layers.py:561-581`） |

**加重项**：`plan_crud._book_context` 取角色是 `characters.limit(40)`，**无排序、无筛选**（`plan_crud.py:49-50`）
→ **死角色、失踪角色、三百章没出现的角色，与主角平等地进上下文**。

##### 为什么 A 档（状态门）并进 7.3 而不是放在这里

casting 是"计划生成时算一次、全篇锁定"，而角色可用性**随章节推进变化**（第 3 章活着，第 50 章死了）。
一次锁定 + 忽略状态 = 必然出现"第 60 章还在用第 3 章选的角色，而他 40 章前就死了"。
**所以状态不是加分项，是 casting 的必需输入** —— 没有它，7.3 交付的是一个会系统性产出连续性错误的选角器。

##### 状态模型定稿（四态）

| 状态 | 含义 | 选角规则 |
|---|---|---|
| **在世** | 正常 | 正常参与选角，可直接召回 |
| **蛰伏** | 久未出场（超阈值章数） | **可入选，但必须写明回归理由**（不禁"久别重逢"，但强制交代） |
| **离场** | 明确离开（离队/远走/失踪/飞升） | 召回须交代回归理由；**用户原提"虚死"即此态**（改名以避免实现者真按死处理），保留**显式放行** |
| **已死** | 死亡（有明确事件） | **默认不进候选池**；回忆/幻象/夺舍等需**显式标记**才放行 |

##### 字段定稿：派生 vs 抽取要分工（关键）

| 字段 | 来源 | 理由 |
|---|---|---|
| `last_seen_chapter` / `appearance_count` | **纯派生**（扫 `chapter_memories.characters` 统计，可重算） | **绝不能靠 LLM** —— 统计能重算、能验证 |
| `status` | **抽取 + 人工可改** | "最后出场"≠"死了"（可能是闭关/失踪/退居幕后），纯统计推不出来 |
| `disappear_mode` / `disappear_chapter` / `disappear_evidence` | **窄任务抽取 + 人工可改** | 见下 |
| `status_evidence` | 抽取（哪一章、哪句话） | **可解释、可回溯、可翻案** |

##### `disappear_mode`：用户最有价值的一条（把模糊概念变成可判定字段）

```
explicit  有具体离场事件（失踪 / 跌落 / 被追杀失散 / 死亡）
silent    只是后续章节没再写他
unknown
```
`explicit` 再分 **`dead` / `missing` / `departed`**（三者的放行条件完全不同：死亡回归只能靠回忆/幻象/夺舍；
失踪回归天经地义）。

**为什么关键**：`silent` 退场的人回归几乎不需要理由（他就是没被写），
`explicit` 失踪/死亡的人回归**必须交代**。没有这个区分，"编回归理由"根本无从下手。
**注意区分**："被人提到/回忆"**不算**出场，也不算消失事件（用户明确要求）。

##### 回归理由：确定性预取，**不走 FC**

| 步骤 | 由谁决定 | 需要模型自主决策吗 |
|---|---|---|
| 谁需要回归理由 | casting 已算出（`needs_reentry_note`） | 不需要 |
| 他哪一章消失 | 台账已算出 | 不需要 |
| 消失后到现在的篇章梗概 | 一个 SQL + 按实体命中补取细节章 | 不需要 |

**三步全是确定性查询 → 用 FC 等于把确定性任务交给不确定性组件**，与 `chapter.py:254` 当年放弃
AI 两阶段 `LOAD_REFS`（"本地模型在该协议上不稳定，时而全不加载、时而一股脑全加载"）是同一个坑；
也是 03 里"那条链要的是稳定不是自主性"的同一判断。

**FC 在这个场景的正当用武之地**（不否认）：缺席期很长（如 80 章）导致材料超窗口时，模型可能需要
**多轮取数**（先看篇章梗概 → 发现某几章关键 → 再取细节）——**步数不确定**才是 FC 的场景。
即便如此，**后端两级预取**也能做，仍保持确定性。→ **本功能用预取实现，FC 留给 7.6 的 `retrieve_templates`。**

##### 回归理由的来源优先级（2026-09-12 用户认可后补充）

```
① 他自己的未回收伏笔   ← 埋-收结构天然成立；伏笔表现成（buried_chapter + status）；
                          且 01 已把伏笔列为"长篇最核心价值" → 几乎零额外成本
② 缺席期的世界线事件   ← 势力在动 / 大事记
③ 纯新编               ← 兜底，质量最差
```
**把"编理由"变成"收伏笔"**，比让模型凭空编高一个档。

##### 新角色引入链路（补上断掉的一环）

现状断在中间，需要补的是 `new_chars` → `pending_entities` 这一环：

```
计划生成 → new_chars（只在计划里）
   ↓ 拍板        ← 【缺这一环：落 pending_entities】
待确认实体（现成机制，当前只接写后摄取的 new_entities）
   ↓ 作者确认
写 characters（带 first_appearance / status=alive）
```
三条约束（防止"引入新角色"变成"人物爆炸"）：
1. **必须绑定功能位** —— 即"他是来补哪个 cast 槽位"。与 7.3 的 `unmatched` 槽位**天然是同一条链**：
   槽位没匹配到 → 问作者"新建角色 / 改选现有角色"。新角色因此**天生带 role_desc 和 slot**，不是凭空的名字。
2. **限额**：一篇新角色上限 **≤3**（LLM 没有成本感，不设限会一篇造五个）。
3. **必须指定首次登场章**（`first_appearance` 写死），否则会出现"第 2 章就用了第 7 章才登场的人"。

##### 篇间交接校验（最便宜、收益最高，零 LLM 成本）

1. 规划前取**上一篇最后 1~3 章**的 `chapter_memories.characters` → "上场遗留名单"；
2. 计划生成后做**差集**：遗留名单里的人，本篇**既不在 `recall_chars` 也不在任何行的角色里** →
   **警告**（非报错）并列出，让作者三选一：交代他离场 / 安排他出场 / 忽略；
3. 同一位置做**硬拦截**：`已死` 角色出现在 `recall_chars` → **直接剔除并报告**
   （复用现在"查无此人剔除"的同一位置）。

##### 分档（避免一次吞掉）

| 档 | 内容 | 归属 |
|---|---|---|
| **A** | `last_seen` 派生 + `status` 字段 + casting 候选池按状态过滤 | **并入 7.3**（casting 的前置依赖） |
| **B** | 新角色引入链路（`planned_chars` 落地 + 限额 + 首登场章）+ 篇间交接差集校验 | **本小节 7.3.5** |
| **C** | `status` 自动抽取（改写后摄取 prompt）+ 状态可视化 UI | 以后再说（成本高，先靠作者手改也能跑） |

##### 验收（对应 docs/01 §4 门槛 8）

`已死` 角色不出现在计划中；久缺席角色被召回时**带可解释的回归理由**（且理由优先来自未回收伏笔）；
上一篇在场、本篇消失的角色被显式交代。三者均为**代码层校验**，不依赖模型自觉。

##### 诚实边界（不得标 ✅）

1. **一条都验不了**：`chapters` = 0 行、`chapter_memories` 33 行、`characters` 2 个空角色
   → 只能造临时项目验逻辑，效果验不了。
2. **`disappear_mode` 抽取未实测**：新概念，需先小样本真机测 prompt（会不会把 `silent` 判成 `explicit`），
   再决定它进哪一档。
3. **"多少章算久"只能拍**：无真实长篇数据 → 无分布可标定，标 ⚠️ 存疑，等有真实长篇再标。
4. **本书没有"故事内时间"字段**（只有章号 + 空间几何）→ "与时间线对应"做不到字面意义，
   最小实现是"用缺席期梗概的**因果**定位"；真时间线属另一模块，**不建议现在开**。

##### 7.3.5 B 档实施记录（2026-09-13 ✅）

按上文定稿方案全量落地，**单测层验证**（21 例新增，全量 345 全绿；app 装配 168 路由核对无误）。

**交付物对照（方案 → 实现）**：

| 方案条目 | 实现 | 关键决策落点 |
|---|---|---|
| 新角色引入链路（补断环） | 新表 `plan_chars` + `plan_crud` 落地/确认/更新三链路 + routers 3 端点 | **独立表而非塞 `chapter_memories.new_entities`**：那条链按 chapter 维度组织，计划新角色绑的是功能位 + 篇内行号，硬塞要造伪记忆行。约束①②③全实现：≤3（`PLANNED_CHAR_LIMIT`，超出剔除进 `new_chars_dropped`）、绑功能位但**不自动猜绑定**（`unmatched_slots` 报告给作者关联——猜测性映射会误导）、`first_appearance` 篇内行号必填 |
| 确认建卡（拍板后接手） | `confirm_planned_char`：建卡回链 `character_id`、置 confirmed | **重名跳过不重复建卡**（同名即同人，重复建卡=两份互相矛盾的设定）；幂等（已 confirmed 直接返回）；brief 兜底 `补「{slot}」功能位的新角色` |
| 重生成不蒸发作者意志 | 重落**只删 pending**，confirmed/dismissed 保留（同名跳过） | 与写后摄取确认链同一纪律 |
| 篇间交接差集 | `carryover_check`：上次写作位置 = **非本篇**的全局最大 chapter_no 记忆（网文顺序写作假设），取前 3 条 characters 并集 = 遗留名单；差集 = 遗留 − (recall_chars ∪ new_chars ∪ casting_names) | **警告非报错**（返回 carryover_names，三选一留给作者）；防自指：`or_(article_id.is_(None), article_id != 本篇)` —— 本篇自己的记忆不算"上一篇遗留" |
| 回归理由材料包（确定性预取） | `casting_crud.reentry_material` + `GET …/characters/{cid}/reentry-material` | **不走 FC**（三步全确定，边界约束 §Phase 6）；优先级代码化为 `priority` 字段：①未回收伏笔（`related_ids` 含 cid 或 description 含角色名，`status != resolved`）→ ②缺席期世界线（`chapter_memories` 实体命中 + arc_digest）→ ③纯新编兜底。**`_reentry_for_plan` 只对 `needs_reentry_note` 且有 character_id 的选角行触发** |
| 计划尾部统一 | `_finalize_plan`：选角 → 新角色落地 → 交接差集 → 回归材料，两个生成分支共用 | 全程 try/except 容错（连续性辅助绝不拖垮主业务）；carryover/reentry 存 plan JSON 时 **deepcopy 断共享引用**（7.2 踩过的 JSON 共享引用坑，防重落时互相污染） |
| 级联删除 | `project_crud` / `article_crud` / `volume_crud` 三入口接入 `plan_chars` | 7.2 的教训（漏一处留孤儿） |

**踩坑两笔**：

1. **Edit 插入新 ORM 类吞掉下一个类声明行**：`old_string` 含 `class BookAliasORM(Base):` 而 `new_string`
   没补回 → BookAliasORM 的 docstring/`__tablename__`/全部列被吸收进 `PlannedCharORM` 类体
   （Python 后面的 `__tablename__` 赋值覆盖前面），`plan_chars` 的插入实际写进了 `book_aliases`
   → `NOT NULL constraint failed: book_aliases.book_name`，8 个测试连带失败。
   **教训**：在两个类声明之间插新类时，old/new 边界必须把下一个 `class` 行原样带上。
2. **`create_character` 返回的是 ORM 对象不是 dict**：`ch["id"]` → `ch.id`。写集成代码前先看
   被调函数的返回形状，别想当然。

**诚实边界（不得标"真机验证"）**：

1. **端到端效果验不了**：库里 `chapters`=0、`chapter_memories` 33 行、`characters` 2 个空角色
   → 逻辑用临时项目单测验证，"差集真的能拦住忘写的人""回归理由真的可解释"需等真实长篇。
2. **`first_appearance` 与全局章号两套编号**：篇内行号是生成时唯一可得的事实；建卡后真实出场章
   由写后摄取 `last_seen_chapter` 派生。两套编号**刻意不混用**，前端展示时需注意标注。
3. **"多少章算久缺席"仍未标定**（同方案 §诚实边界 3）：等真实长篇有分布再标。
4. C 档（`status` 自动抽取 + 状态可视化 UI）未动，按分档表"以后再说"。

---

## 3. 战略待拍板（别急着做，先想清楚）

| 议题 | 现状 | 待定 |
|---|---|---|
| **🔴 检索架构升级** | **A 线 A1~A6 全部完成、激活并真机验收（2026-09-10）**：embedding 客户端 / 向量存储双实现 / 摄取钩子 / Hybrid RRF 融合 / rerank 重排 / 实体图一跳扩展 / 按需加载 e2e 全链覆盖，真机 e2e 通过（含修掉跨项目稀释与孤儿向量两个真缺陷） | 无必须项；可选增强见 Phase R 末行 |
| **🟡 e2e 覆盖按需加载流程** | **已完成（2026-09-10，A6）**：`tests/e2e/test_on_demand_load.py` 覆盖商讨两阶段（LOAD_REFS/LOAD_SETTING 真加载 + 短路）+ 章节 hybrid top-up，9/9 通过；答案取"不可猜"事实防幻觉（剑碑名「照胆」、灵石兑率 8768） | 无（后续若要测 toolcall 策略 B，需先实现 `ToolCallSelection`） |
| **🔴 工作流该做成什么** | 对当前实现**一点都不满意**；3 个工作流 / 7 次运行，用得少 | 在想清楚之前：**不投入、不删除、不扩展** |
| ~~**套路模板的正确形态**~~ | 7 条硬编码、题材绑定、不可编辑 | **已拍板（2026-09-11）**：结构化模板库（`phase→beat→variants`，跨书 variants 溯源），从爬取小说凝练入库 —— 见 Phase 7 |
| **工作流与主链路双轨** | 工作流生成的章节**不触发写后摄取**，与主链路行为不一致 | 收敛为"主链路唯一，工作流只做编排外壳"？ |
| ~~**4 级结构 小说→卷→篇→章**~~ | 20 卷 + 22 篇 装 3 章有效内容（其余全是孤儿） | **已拍板（2026-09-11）：篇 = 规划单元**（模板挂载 + 章计划 + 拍板后驱动生成）—— 见 Phase 7 |
| **前端是否全量重写** | God Store 494 行、MainLayout 690 行、单文件组件 873 行、无 TS 无 composable | 后端继承 + 前端重写的混合方案是否采纳？（须先冻结接口契约） |

### 3.1 ~~孤儿数据~~（✅ 已于 2026-09-10 清理，见 01 §2 与 02 §6.2 —— 非待办，留档备查）

2026-09-09 实测：数据库里 **62%~91% 是孤儿**，来自 21 部已删除的作品。

| 表 | 总 | 有效 | 孤儿 |
|---|---|---|---|
| `volumes` | 20 | 2 | **18** |
| `articles` | 22 | 2 | **20** |
| `chapter_memories` | 33 | 4 | **29** |
| `reference_docs` | 42 | 3（+18 全局） | **21** |
| `chapters` | 8 | 3 | **5** |
| `discussion_load_logs` | 8 | 3 | **5** |

**已验证：当前删除逻辑是干净的**（新建 → 塞入卷/篇/章/角色/地点 → 删除 → 残留 0）。
所以这是 **2026-08-13 修复级联删除之前**留下的历史残留，**不需要改代码，只需要清一次数据**。

清理前必须：① 备份 db 文件；② 逐表确认 `project_id` 不在 `projects` 且不等于 `__global__`。

**2026-09-09**：已备份 + 已全量导出（98 行）到 `.workbuddy/memory/_orphan_export/`，DB 未动。

**2026-09-10 已清理完成**（用户拍板「清掉」）：

| 表 | 删除行数 |
|---|---|
| `articles` | 20 |
| `chapter_memories` | 33 |
| `reference_docs` | 21 |
| `volumes` | 18 |
| `chapters` | 5 |
| `discussion_load_logs` | 5 |
| **合计** | **102** |

- 流程：**备份**（`novel_agent.db.bak.20260910_135942`）→ **导出**（`.workbuddy/memory/_orphan_export/2026-09-10_20260910_135942/orphans.json`）→ 事务删除 → 校验
- 校验结果：孤儿残留 **0** ✅；全局资料池 `reference_docs` **18** 份未动 ✅；全局向量 `vector_chunks` = `vec_index` = **133** 未动 ✅
- 清理后数据全景：1 作品 / 1 卷 / 1 篇 / 1 章 / 18 份全局资料 / 133 向量块 / 21 条全局设定

⚠️ **清理时必须豁免 `__global__`**：它不是 `projects` 行，若按「project_id 不在 projects 里」判孤儿，会误删 18 份全局资料 + 133 个全局向量块（全局检索直接报废）；`discussion_messages` 的 25 行也全是全局对话，同理。

### 3.2 多 agent 协同方案（2026-09-09 决策：轻量版）

详见 `docs/07-任务分发手册.md`。

**最终决策（2026-09-09，推翻上一版"专用调度对话"方案）**：
- **主力单干为主**：默认所有活由主力 AI（WorkBuddy 写码）自己做
- **任务积压才分发**：只有"一下要改的任务多"时，主力 AI 拆出**可独立、文件不重叠**的子任务，生成「投递单」，用户复制粘贴给 TRAE / Zcode / OpenCode，当"邮差"
- **废弃**：专用调度 WorkBuddy 对话、任务卡文件系统、调度状态机（投递单 + 用户粘贴即可，零基建）

**为什么放弃现成编排框架**（已调研 Agent Orchestrator / AI-Coding-Tools-Collaborative / um-agent-orchestration 等）：它们都要求 agent 是**可被命令行 spawn 的 CLI**——四 agent 中只有 OpenCode 满足，WorkBuddy / TRAE / Zcode 均不可编程驱动。换血成 CLI agent 与用户免费额度策略冲突，不划算。

**保留不变的**：模型能力分级（🟢GLM-5.3-flash / 🟡deepseek-v4-flash·混元4 / 🔴轻量免费模型）、共享真相层（docs/02 + 06 + CHANGELOG 为唯一真相，agent 文档只记局部）、弱模型只接轻量外围活。

**何时重启框架调研**：① 开始并发投递（不等 A 回就发 B）② 多 agent 频繁改同一批文件 ③ 主力换成可 CLI 驱动 agent。三条件同时出现再说（届时看 Agent Orchestrator 类，支持 OpenCode + git worktree 隔离）。

---

## 4. 不做清单（已否决，别再提）

- ❌ 全量重写新仓库（理由见 §0）
- ❌ 多用户 / 登录 / 云端部署（见 01 §5）
- ~~❌ 向量检索 / RAG~~ → **已撤销不做限制，且已实施完成**（Phase R A 线 A1~A6，2026-09-10 真机验收。见 01 §5.1；实施记录已归档）
- ❌ 文风 LoRA 微调
- ❌ 移动端适配
- ~~❌ 套路模板库~~ → **已撤销**（见 01 §3 ⚠️ 套路模板修正段）：流水线网文最需要套路模板，方向是可编辑/题材中立的结构化模板，先冻结待考究
- ❌ 工作流扩展（01 §5.2：三不原则——不投入/不删除/不扩展）